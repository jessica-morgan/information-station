// test for links

// test('<TopMenu /> contains a link', () => {
//   // expect 'Add Food'
//   const expected = 'Add Food'
//   // assign wrapper to render TopMenu component wrapped in memoryrouter
//   const wrapper = render(<MemoryRouter><TopMenu/></MemoryRouter>)
//   // assign actual to wrappers text
//   const actual = wrapper.text()
//   // expect TupMenu text to match 'Add Food'
//   expect(actual).toMatch(expected)
// })

// Returns a string of the rendered text of the current render tree. This function should be looked at
// with skepticism if being used to test what the actual HTML output of the component will be. If that
// is what you would like to test, use enzyme's render function instead.
// Note: can only be called on a wrapper of a single node.
